import ProfileImage from '../images/title.png';

export const Header = () =>{
  return(
    <header className="main-cover" >
      <img className="page_title" src={ProfileImage} alt="" />
    </header>
  )
}